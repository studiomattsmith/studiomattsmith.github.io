// Generated by CoffeeScript 1.6.2
(function() {
  var Main;

  requirejs.config({
    baseUrl: '/assets/js/libs',
    paths: {
      app: '../app',
      backbone: 'backbone'
    },
    shim: {
      backbone: {
        deps: ['jquery', 'underscore'],
        exports: 'Backbone'
      }
    }
  });

  require(["jquery", "underscore", "backbone"], function() {
    return Main.init();
  });

  Main = {
    init: function() {
      return require(["app/views/index"], this.extendViews);
    },
    extendViews: function() {
      var _this = this;

      return _.each($('.js-extend-view'), function(el) {
        var $el, v, view, viewName;

        $el = $(el);
        viewName = $el.data('view');
        if (viewName === null || viewName === void 0) {
          return;
        }
        view = require("app/views/index");
        v = new view({
          el: el
        });
        return $el.removeClass('js-extend-view');
      });
    }
  };

}).call(this);
